---
title: Правка цепочки
description: Отредактировать сохранённую цепочку с помощью ИИ в новую версию по инструкции на естественном языке.
sidebar:
  order: 2.5
---

`care revise` — это ИИ-двойник команды [`care generate`](/ru/care/cli/generate-run/):
вместо того чтобы строить цепочку с нуля, она передаёт MAGE *уже сохранённую*
цепочку и просит внести минимальную точечную правку, описанную обычными словами.
В TUI ей соответствует команда `/revise`.

## `care revise <chain_id> <change>…`

MAGE планирует наименьшую правку, которая выполняет вашу инструкцию. **По
умолчанию команда лишь показывает этот план** — ничего не сохраняется. Чтобы
записать результат как **новую версию** той же цепочки, добавьте `--yes`.

```bash
care revise weather "use Celsius and add a 3-day forecast"
care revise weather "use Celsius and add a 3-day forecast" --yes
```

| Флаг | По умолчанию | Назначение |
| --- | --- | --- |
| `<change>…` | — | Что изменить, на естественном языке (`nargs=+`, поэтому возьмите в кавычки). |
| `--channel NAME` | `latest` | Версионный канал для чтения и записи. |
| `--mode fast\|deep` | — | Переопределение режима MAGE (по умолчанию — собственный дефолт MAGE). |
| `--yes` | выкл. | Сохранить отредактированную цепочку как новую версию (по умолчанию — только предпросмотр). |
| `--json` | выкл. | Вывести отредактированную цепочку как JSON вместо сводки плана. |

При подтверждении флагом `--yes` новая версия **наследует имя исходной цепочки**
и записывает `change_summary="revise: <инструкция>"`, поэтому правка наглядно
видна в [`care lineage`](/ru/care/cli/memory/) и в истории цепочки.

:::note[Сначала предпросмотр]
Без `--yes` вы увидите запланированную правку и строку
`Preview only — re-run with --yes to save this as a new version.` Это повторяет
логику подтверждения-перед-сохранением из TUI. Если MAGE решит, что менять
ничего не нужно, команда сообщит `MAGE proposed no changes.` и завершится без
ошибок.
:::

Если ссылка `<chain_id>` неоднозначна, `revise` перечислит кандидатов и попросит
перезапустить команду с явным id. При ошибке `401`/`403` вместо сырого
сообщения выводится понятная подсказка про истёкший токен.

---

## Смотрите также

- [Генерация, запуск и воспроизведение](/ru/care/cli/generate-run/) — создание и выполнение цепочек.
- [Memory и Библиотека](/ru/care/cli/memory/) — просмотр версий, родословной и истории.
